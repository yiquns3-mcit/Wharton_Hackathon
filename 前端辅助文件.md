以下是完整清单，以前端视角描述每个接口和工具函数：

---

# API 路由完整清单

---

## 1. `GET /api/hotels`

**触发时机：** 页面一和页面二加载时，填充「选择酒店」下拉菜单。

**前端需要传入：** 无（直接 fetch，无参数）

**返回数据（数组，每项是一家酒店）：**
```json
[
  {
    "eg_property_id": "12345678",   // 酒店唯一ID，后续所有接口都用这个
    "city": "上海",
    "province": "上海",
    "country": "中国",
    "star_rating": 4.5,             // 酒店星级，数字
    "guestrating_avg_expedia": 8.2  // Expedia 平均评分，数字
  }
]
```
**注意：** 下拉菜单建议显示 `city + country + star_rating`，`eg_property_id` 作为 option 的 value 传给后续接口。

---

## 2. `GET /api/hotels/[id]/questions`

**触发时机：** 页面一选择酒店后立即调用，加载 AI 引导问题（**不会调用 OpenAI，只是读缓存**，速度快）。

**前端需要传入：** URL 路径中的酒店 ID，例如 `/api/hotels/12345678/questions`

**返回数据：**
```json
{
  "ai_questions": [
    { "feature": "roomquality",  "question": "您觉得房间的整体质量如何？" },
    { "feature": "valueformoney","question": "您认为此次住宿的性价比怎么样？" },
    { "feature": "checkin",      "question": "入住办理是否顺畅？" }
    // 最多 5 条
  ],
  "top_features": ["roomquality", "valueformoney", "checkin"],  // 与上面一一对应的内部字段名
  "last_updated": "2026-04-10T08:00:00.000Z"  // 可用来显示「数据更新时间」
}
```
**注意：** 如果该酒店尚未初始化（管理员还没跑批量分析），`ai_questions` 返回空数组 `[]`，前端需要有「暂无引导问题」的兜底状态。每条 `question` 字段是给用户看的中文问题，`feature` 是提交评分时对应的 key。

---

## 3. `POST /api/reviews`

**触发时机：** 用户点击「提交评论」按钮。

**前端需要传入（JSON body）：**
```json
{
  "eg_property_id": "12345678",        // 必填，选择的酒店 ID
  "rating": {                          // 必填，15 个固定字段，未评分填 0
    "checkin": 4,
    "overall": 0,
    "service": 5,
    "location": 0,
    "roomcomfort": 3,
    "roomquality": 0,
    "communication": 0,
    "onlinelisting": 0,
    "valueformoney": 4,
    "hotelcondition": 0,
    "ecofriendliness": 0,
    "roomcleanliness": 5,
    "roomamenitiesscore": 0,
    "convenienceoflocation": 0,
    "neighborhoodsatisfaction": 0
  },
  "review_title": "干净舒适，值得推荐",  // 可选，可传 null
  "review_text": "房间很干净，服务态度好...", // 可选，可传 null
  "images": [                          // 可选，Supabase Storage 的公开 URL 数组
    "https://xxx.supabase.co/storage/v1/object/public/review-images/xxx.jpg"
  ]
}
```
**返回数据（成功，HTTP 201）：**
```json
{
  "ok": true,
  "review_id": "a1b2c3d4-..."  // 这条评论的 UUID，前端一般不需要用
}
```
**注意：** 提交成功后，AI 分析在后台异步进行，**前端不需要等待，直接跳转成功页**。`rating` 中 `0` 代表「用户没有评这项」，不是零分，前端未评分的项直接传 `0` 即可。

---

## 4. `POST /api/ai/polish`

**触发时机：** 用户点击「✨ AI 润色」按钮，把草稿文字传给 AI 改写后回填到文本框。

**前端需要传入（JSON body）：**
```json
{
  "draft_text": "房间还行，服务一般，就是有点贵",  // 必填，用户写的草稿
  "ratings": {                                    // 可选，当前用户已打的星级
    "roomquality": 3,
    "valueformoney": 2
  }
}
```
**返回数据：**
```json
{
  "polished": "入住体验整体属于中规中矩水平。房间设施基本满足需求，但整体质量感与同价位酒店相比略显平淡。服务态度尚可，然而效率和主动性有待提升。考虑到价格定位，性价比偏低，预算有限的旅客建议谨慎考虑。"
}
```
**注意：** 返回的 `polished` 直接回填到 textarea，让用户再做修改后提交。**不会自动保存**，只是临时改写。

---

## 5. `POST /api/analyze`（内部接口，前端一般不直接调用）

**触发时机：** 由 `POST /api/reviews` 在服务端自动触发，前端不需要手动调用。

**传入：** `{ "review_id": "uuid..." }`

**返回：** `{ "ok": true, "review_id": "...", "text_analysis": { "roomquality": 4, ... } }`

---

## 6. `POST /api/batch-analyze`

**触发时机：** 管理员在 Dashboard 点击「开始批量分析历史评论」按钮。**这个接口返回的是 SSE 流（Server-Sent Events），不是普通 JSON**，前端需要用流式读取实时更新进度条。

**前端需要传入（JSON body）：**
```json
{
  "eg_property_id": "12345678"  // 必填，要分析哪家酒店的历史评论
}
```

**返回：SSE 事件流**，前端依次收到这些事件：

| 事件 `type` | 触发时机 | 包含字段 | 用途 |
|---|---|---|---|
| `start` | 开始时发一次 | `total: 50` | 初始化进度条总数 |
| `progress` | 每处理完一条 | `processed: 3, total: 50, failed: 0` | 更新进度条 `processed/total` |
| `done` | 全部完成 | `processed: 50, total: 50, failed: 2` | 关闭进度条，刷新 BI 数据 |
| `error` | 严重错误 | `message: "..."` | 显示错误提示 |

**SSE 读取示例（前端代码结构）：**
```ts
const res = await fetch('/api/batch-analyze', { method: 'POST', body: ... })
const reader = res.body.getReader()
// 循环 reader.read() → 解析 "data: {...}\n\n" → 更新 UI
```

---

## 7. `GET /api/bi/[id]`

**触发时机：** 管理员在 Dashboard 选择酒店后，加载该酒店的所有 BI 分析数据。

**前端需要传入：** URL 路径中的酒店 ID，例如 `/api/bi/12345678`

**返回数据（完整结构）：**
```json
{
  "eg_property_id": "12345678",
  "total_reviews": 200,                 // 总评论数，用于 Summary 卡片
  "text_analysis_coverage": 0.73,       // 0~1，73% 的评论已完成 AI 分析，用于卡片
  "last_updated": "2026-04-10T08:00:00.000Z",  // 上次更新时间，用于卡片

  "weights": { "w1": 0.4, "w2": 0.3, "w3": 0.3 },  // 当前权重，用来初始化滑块

  "priority_scores": {           // 每个特征的综合优先级分数，用于排行榜
    "checkin": 0.6821,
    "valueformoney": 0.6543,
    "roomquality": 0.5912,
    // ...共 15 个特征
  },
  "top_features": [              // 按得分降序排列的前 5 名特征名，用于高亮显示
    "checkin", "valueformoney", "roomquality", "location", "service"
  ],

  "dimensions": {
    "missing_rate": {            // 各特征的缺失率（0~1），用于「缺失率」柱状图
      "checkin": 0.82,
      "overall": 0.05,
      // ...
    },
    "time_confidence": {         // 各特征的时间置信度（0~1），用于「时间置信度」柱状图
      "checkin": 0.31,
      "overall": 0.78,
      // ...
    },
    "text_coverage": {           // 各特征的文本覆盖率（0~1），用于「文本覆盖率」柱状图
      "checkin": 0.12,
      "overall": 0.65,
      // ...
    }
  }
}
```
**注意：** `dimensions` 里的值都是 `0~1` 的小数，画图时乘以 100 转成百分比显示。

---

## 8. `POST /api/bi/[id]`

**触发时机：** 管理员拖动权重滑块后点击「应用权重 & 重新计算」。

**前端需要传入（JSON body）：**
```json
{
  "w1": 0.5,   // 缺失率权重
  "w2": 0.3,   // 时间衰减权重
  "w3": 0.2    // 文本覆盖权重，三个必须加起来等于 1.0
}
```
**返回数据（与 `GET /api/bi/[id]` 基本相同，用新权重重新计算后的结果）：**
```json
{
  "ok": true,
  "orderChanged": true,          // 优先级排名顺序是否改变（影响引导问题是否重新生成）
  "priority_scores": { ... },
  "top_features": [ ... ],
  "dimensions": { ... },
  "total_reviews": 200,
  "text_analysis_coverage": 0.73,
  "weights": { "w1": 0.5, "w2": 0.3, "w3": 0.2 }
}
```
**注意：** 收到响应后直接用返回数据刷新所有图表，不需要再调 `GET /api/bi/[id]`。

---

## 9. `POST /api/ai/questions`（内部接口，前端一般不直接调用）

**触发时机：** 由后端在权重变化导致排名变化时自动调用，前端不需要手动触发。

---

# lib/ 工具函数清单

---

## `lib/supabase.ts` — `supabase`

| | |
|---|---|
| **导出** | `supabase`（Supabase 客户端实例）|
| **用途** | 页面一图片上传时，前端直接调用此客户端上传文件到 Supabase Storage |
| **前端使用场景** | `supabase.storage.from('review-images').upload(path, file)` → 拿到 `publicUrl` → 传给 `POST /api/reviews` 的 `images` 字段 |

---

## `lib/openai.ts` — `openai`

| | |
|---|---|
| **导出** | `openai`（OpenAI 客户端实例）|
| **用途** | 仅在服务端 API 路由内部使用，前端代码不直接调用 |

---

## `lib/scoring.ts` — `calculatePriorityScores`

| | |
|---|---|
| **输入** | 一家酒店的所有评论数组 + 权重配置 `{w1, w2, w3}` |
| **输出** | `priority_scores`（每个特征的综合分）、`top_features`（前 5 名列表）、`dimensions`（三维度原始数据）|
| **用途** | 纯计算函数，在 `GET /api/bi/[id]` 和 `POST /api/bi/[id]` 内部调用，前端不直接使用 |
| **重要常量** | `RATING_FEATURES` — 15 个固定特征名的数组（`checkin`, `overall`, `service` 等）|

---

## `lib/prompts.ts` — 三个 prompt 构建函数

| 函数名 | 用途 |
|---|---|
| `buildPolishPrompt(draftText, ratings)` | 构建 Scene 1 润色 prompt，在 `/api/ai/polish` 内部调用 |
| `buildQuestionsPrompt(topFeatures, hotelDesc)` | 构建 Scene 2 生成引导问题 prompt，在 `/api/ai/questions` 内部调用 |
| `buildAnalysisPrompt(reviewText, hotelDesc, features)` | 构建 Scene 3 文本分析 prompt，在 `/api/analyze` 和 `/api/batch-analyze` 内部调用 |
| `stripMarkdownFences(raw)` | 清理 OpenAI 返回文本中偶发的 markdown 代码块标记，内部工具，前端不用 |

---

## `lib/hotelAnalysisUpdater.ts` — 三个异步函数

| 函数名 | 输入 | 输出 | 用途 |
|---|---|---|---|
| `fetchHotelDescription(propertyId)` | 酒店 ID | 该酒店完整描述信息对象 | 内部工具，给 Scene 2/3 提供酒店描述数据 |
| `fetchHotelReviews(propertyId)` | 酒店 ID | 该酒店所有评论的评分 + text_analysis 数组 | 内部工具，给评分计算提供原始数据 |
| `updateHotelAnalysis(propertyId, weights?)` | 酒店 ID + 可选新权重 | `{priority_scores, top_features, orderChanged}` | 核心自动更新逻辑：重算优先级 → 比较排名是否变化 → 按需重生成引导问题 → 写入数据库 |

---

# 快速参考：前端需要调用哪些接口

| 页面 | 时机 | 调用接口 |
|---|---|---|
| 页面一 | 加载时 | `GET /api/hotels` |
| 页面一 | 选完酒店 | `GET /api/hotels/[id]/questions` |
| 页面一 | 点「AI 润色」 | `POST /api/ai/polish` |
| 页面一 | 上传图片 | 直接调 `supabase.storage`（不走 API 路由）|
| 页面一 | 点「提交」 | `POST /api/reviews` |
| 页面二 | 加载时 | `GET /api/hotels` |
| 页面二 | 选完酒店 | `GET /api/bi/[id]` |
| 页面二 | 点「应用权重」 | `POST /api/bi/[id]` |
| 页面二 | 点「批量分析」 | `POST /api/batch-analyze`（SSE 流读取）|